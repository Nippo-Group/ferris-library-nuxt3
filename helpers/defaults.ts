import { DefaultsInstance } from "vuetify/lib/framework.mjs";

export const defaults: DefaultsInstance = {
  VAppBar: {
    elevation: 0,
  },
  VAlert: {
    variant: "tonal",
    density: "compact",
    border: "start",
  },
};
